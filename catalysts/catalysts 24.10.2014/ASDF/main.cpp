#include <iostream>
using namespace std;

#define pad_h 150

double my_pos,my_lm,op_pos,op_lm,ball_x,ball_y;
double v_x,v_y;
string cmd;

int main()
{
    /*while(1)
    {
        cin>>cmd>>my_pos>>my_lm;
        cin>>cmd>>op_pos>>op_lm;
        cin>>cmd>>ball_x>>ball_y>>v_x>>v_y;
        cin>>cmd;
        if(v_x>0) ball_y=400;
        if(ball_y>my_pos+pad_h/2.0)
        {
            cout<<"move "<<(min(36.0,ball_y-(my_pos+pad_h/2.0)))<<endl;
        }
        else
        {
            cout<<"move "<<(max(-36.0,ball_y-(my_pos+pad_h/2.0)))<<endl;
        }
        cerr<<my_pos << endl;
    }*/
    while (1)cerr<<"ASDF"<<endl;
    return 0;
}

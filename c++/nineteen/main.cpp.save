#include <iostream>
#include <string>

using namespace std;
string s;
int i;
int nn,ni,ne,nt;
int main()
{
    cin>>s;
    for(i=0;i<s.length();++i)
{
    switch (s[i])
    {
        case 'n': ++nn;break;
        case 'i': ++ni;break;
        case 'e': ++ne;break;
        case 't': ++nt;break;
    }
    ne/=3;
    maxx=ne;
    maxx=min(maxx,ni);
    maxx=min(maxx,nt);
    
    
}
    return 0;
}
